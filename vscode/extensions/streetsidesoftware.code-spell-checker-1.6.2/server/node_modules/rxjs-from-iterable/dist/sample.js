"use strict";
const fromIterable_1 = require("./fromIterable"); // 'rxjs-from-iterable';
const fromIterable_2 = require("./fromIterable"); // 'rxjs-from-iterable';
function uniqueWords(text) {
    const setUniqueWords = new Set(text.split(' '));
    return fromIterable_1.observableFromSet(setUniqueWords);
}
exports.uniqueWords = uniqueWords;
function* genFib() {
    let [a, b] = [0, 1];
    while (true) {
        [a, b] = [b, a + b];
        yield a;
    }
}
exports.genFib = genFib;
function fibonacci(n) {
    return fromIterable_2.observableFromIterable(genFib())
        .take(n);
}
exports.fibonacci = fibonacci;
//# sourceMappingURL=sample.js.map