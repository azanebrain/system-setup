import { AliasedImport } from './Import';
/**
 * Default import. Imports the default exports of a file.
 * (i.e. import foobar from ...).
 *
 * @export
 * @class DefaultImport
 * @implements {AliasedImport}
 */
export declare class DefaultImport implements AliasedImport {
    libraryName: string;
    alias: string;
    start: number | undefined;
    end: number | undefined;
    readonly isNew: boolean;
    constructor(libraryName: string, alias: string, start?: number | undefined, end?: number | undefined);
    /**
     * Clone the current import object.
     *
     * @returns {DefaultImport}
     *
     * @memberof DefaultImport
     */
    clone(): DefaultImport;
}
